# Data Types

## String

You can interpolate strings by using `f''`:

```py
name = "César"

f'Hello, {name}'
```

## Lists

## The ```type()``` function

We can use the ```type()``` function to get the type of a value:

```py
type(2) # int
type(2.4) # float
type(True) # bool
type("Hello World") # str
```